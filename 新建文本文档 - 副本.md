你正在继续 SVT-v2 项目。

当前已有技术报告发现了 Delta-Output Identity Paradox：
- RawTrajectoryKNN v1: clean skill 较低但 identity-test identity 较高，约 0.830
- RawDeltaKNN v2: clean skill 更高，但 identity-test identity 接近随机，约 0.530
- 多数 learned models 在 identity-test 上接近随机
- 但报告中存在一个必须审计的问题：不能简单说“所有模型 identity 都低于随机”，因为 RawTrajectoryKNN v1 明显高于随机

本阶段不要进入 v3 大实验。
不要新增复杂模型。
不要加入 LLM。
不要写新理论。
本阶段只做：SVT-v2.1 Leakage and Identifiability Audit。

核心目标：
1. 解释 RawTrajectoryKNN v1 identity=0.830 的来源；
2. 检查它是否来自 object order、initial position、swap time、occlusion location、absolute position、train/test overlap 或 metric bug；
3. 判断当前 featureless identity-swap task 对普通观察者是否可辨识；
4. 生成审计报告，修正 v2 中过强结论；
5. 为 v3 提供明确设计建议。

========================
项目已有结构
========================

当前项目大致包含：

configs/
envs/
baselines/
models/
metrics/
scripts/
data/
results/
reports/

已有关键文件可能包括：
- envs/motion_world.py
- data/generate_2d_motion.py
- baselines/knn_retriever.py
- baselines/knn_retriever_v2.py
- metrics/identity_metrics.py
- metrics/prediction_metrics.py
- metrics/gated_svt_score.py
- scripts/run_knn_attack.py
- scripts/run_knn_attack_v2.py
- reports/full_report.md 或 COMPLETE_TECHNICAL_REPORT.md

请先检查现有代码，不要重复实现已有功能。
优先复用现有 generator、KNN baseline 和 metric。

========================
新增目录
========================

请创建：

results/svt_v2_1_audit/
  object_order_audit.csv
  disjoint_init_audit.csv
  swap_randomization_audit.csv
  label_permutation_audit.csv
  position_ablation_audit.csv
  nn_source_analysis.csv
  identifiability_probe.csv
  audit_summary.csv
  nn_distance_plot.png
  audit_fingerprint.png
  README.md

新增脚本：

scripts/run_svt_v2_1_audit.py

如有必要，也可以新增：

data/generate_audit_datasets.py
metrics/leakage_metrics.py

但不要大规模重构。

========================
总体实验要求
========================

请实现 7 个审计实验：

A. Object Order Randomization Audit
B. Train/Test Disjoint Initial Position Split
C. Swap Time and Occlusion Location Randomization
D. Identity Label Permutation Sanity Check
E. Absolute Position Ablation
F. Nearest Neighbor Source Analysis
G. Identifiability Probe: featureless vs feature-bearing objects

每个实验都要评估至少以下模型：

1. RawTrajectoryKNN v1
2. RawDeltaKNN v2
3. TranslationNormalizedKNN
4. VelocityOnlyKNN 或 LastVelocityBaseline，如果已有
5. RandomIdentityBaseline

如果某个模型当前不存在，请优先复用已有同类 baseline，不要新增复杂模型。

每个实验至少输出：
- clean_skill
- identity_accuracy
- identity_after_swap 或 identity_test_accuracy
- gated_svt_score，如果可计算
- sample_count
- seed

默认 seeds = [0, 1, 2]
如果运行太慢，先支持 seed=0，并在 README 中说明。

========================
A. Object Order Randomization Audit
========================

目的：
检查 RawKNN v1 的高 identity 是否来自对象顺序泄漏。

实现要求：
1. 在数据生成或加载后，对 train/test observation 和 future 中的 object order 做随机打乱。
2. 打乱必须同步应用到 observed_positions、future_positions、identity labels。
3. 至少测试两种设置：
   - original_order
   - randomized_order

评估：
- RawTrajectoryKNN v1
- RawDeltaKNN v2
- TranslationNormalizedKNN

输出：
results/svt_v2_1_audit/object_order_audit.csv

CSV 字段：
seed
setting
model
clean_skill
identity_accuracy
gated_svt_score

解释规则：
- 如果 randomized_order 后 RawKNN identity 从 0.830 降到接近 0.5，说明原结果可能依赖 object order。
- 如果仍然高，说明 object order 不是主要来源。

========================
B. Train/Test Disjoint Initial Position Split
========================

目的：
检查 train/test 初始位置分布重叠是否让 RawKNN 通过绝对位置检索获得高 identity。

实现要求：
构造至少两种 split：

1. random_split：
   常规随机 train/test。

2. disjoint_init_split：
   train 和 test 的初始位置区域不重叠。
   简单实现方式：
   - train objects 初始化在 arena 左半区或指定网格 cell
   - test objects 初始化在 arena 右半区或不同网格 cell
   - 或者使用 grid partition，确保 train/test initial position bins 不重叠

评估：
- RawTrajectoryKNN v1
- RawDeltaKNN v2
- TranslationNormalizedKNN

输出：
results/svt_v2_1_audit/disjoint_init_audit.csv

CSV 字段：
seed
split_type
model
clean_skill
identity_accuracy
gated_svt_score
mean_initial_position_gap

解释规则：
- 如果 disjoint split 后 RawKNN identity 大幅下降，说明它依赖绝对位置/轨迹邻近检索。
- 如果仍然高，说明可能存在其他真实线索或 metric 耦合。

========================
C. Swap Time and Occlusion Location Randomization
========================

目的：
检查 RawKNN 是否利用固定 swap time、固定 occlusion interval 或固定 occlusion center 模板。

实现要求：
构造至少两种数据设置：

1. fixed_swap_occlusion：
   使用当前默认设置。

2. randomized_swap_occlusion：
   - swap timestep 随机化
   - occlusion start/end 随机化
   - occlusion center 随机化
   - occlusion radius 可轻微随机化

注意：
随机化后仍要保证 identity_test 中存在可比较的 swap/no-swap episode。

评估：
- RawTrajectoryKNN v1
- RawDeltaKNN v2
- TranslationNormalizedKNN
- LastVelocityBaseline 或 VelocityOnlyKNN

输出：
results/svt_v2_1_audit/swap_randomization_audit.csv

CSV 字段：
seed
setting
model
clean_skill
identity_accuracy
gated_svt_score
swap_time_variance
occlusion_center_variance

解释规则：
- 如果 randomized 后 RawKNN identity 明显下降，说明它利用了 swap/occlusion 模板。
- 如果不下降，说明模板不是主要来源。

========================
D. Identity Label Permutation Sanity Check
========================

目的：
检查 identity metric 或 evaluation pipeline 是否有 bug。

实现要求：
1. 保持模型预测不变。
2. 对测试集 identity labels 随机置换。
3. 重新计算 identity accuracy。
4. 至少重复 5 次 permutation，报告均值和方差。

评估：
- RawTrajectoryKNN v1
- RawDeltaKNN v2
- RandomIdentityBaseline

输出：
results/svt_v2_1_audit/label_permutation_audit.csv

CSV 字段：
seed
permutation_id
model
original_identity_accuracy
permuted_identity_accuracy

解释规则：
- permuted_identity_accuracy 应接近 0.5。
- 如果置换标签后 accuracy 仍显著高于随机，说明 identity metric 或评估流程存在严重漏洞。
- 如果 RandomIdentityBaseline 不接近 0.5，也说明评估流程有问题。

========================
E. Absolute Position Ablation
========================

目的：
检查 RawKNN 高 identity 是否主要来自绝对位置，而非真正身份结构。

比较模型：
1. RawTrajectoryKNN v1
2. TranslationNormalizedKNN
3. VelocityOnlyKNN
4. RawDeltaKNN v2

实现要求：
如果 TranslationNormalizedKNN 已有，直接复用。
如果没有，请实现最小版本：
- 对每个 episode，将 observed trajectory 减去最后观测位置或初始中心位置
- KNN 在 normalized trajectory 上检索
- 输出时再按需要 re-anchor

输出：
results/svt_v2_1_audit/position_ablation_audit.csv

CSV 字段：
seed
model
uses_absolute_position
clean_skill
identity_accuracy
gated_svt_score

解释规则：
- RawKNN 高、TranslationNormalizedKNN 掉到随机：绝对位置是主要 shortcut。
- TranslationNormalizedKNN 仍高：说明存在轨迹形状或其他结构线索。
- VelocityOnly 高：说明 identity metric 可能被速度连续性污染。

========================
F. Nearest Neighbor Source Analysis
========================

目的：
解释 RawKNN 每个 test episode 的最近邻来源，判断正确 identity 是否来自更近邻居、相似初始状态或相似 occlusion/swap 模板。

实现要求：
对 RawTrajectoryKNN v1 记录每个 test episode 的最近邻信息：

字段：
test_id
nearest_train_id
nn_distance
identity_correct
test_is_swap
train_is_swap，如果 train 有 swap 信息
initial_position_gap
last_observed_position_gap
velocity_gap
occlusion_center_gap，如果可得
swap_time_gap，如果可得
prediction_mse
identity_label
predicted_identity_label

如果某些字段当前数据中没有，就填 NaN，并在 README 说明。

输出：
results/svt_v2_1_audit/nn_source_analysis.csv
results/svt_v2_1_audit/nn_distance_plot.png

分析要求：
1. 比较 identity_correct=True vs False 的 nn_distance 分布。
2. 比较 correct vs incorrect 的 initial_position_gap。
3. 如果正确样本显著更近，说明 RawKNN 依赖局部检索。
4. 如果 incorrect 和 correct 没差异，需要继续检查 metric 或其他线索。

图：
- x-axis: nn_distance
- y-axis: count or density
- separate correct vs incorrect
- 使用 matplotlib，不要 seaborn

========================
G. Identifiability Probe: Featureless vs Feature-Bearing Objects
========================

目的：
判断当前 featureless swap task 是否对普通观察者可辨识。

实现要求：
构造两类数据：

1. featureless objects:
   - same color
   - same radius
   - no marker
   - 与当前 identity_test 类似

2. feature-bearing objects:
   - object 0 和 object 1 拥有可观测 feature
   - 例如 unique color、marker scalar、shape id
   - feature 必须出现在 observation 和 future 的可用信息中
   - 如果当前模型只接受 position，需要实现一个简单 feature-aware identity baseline，或至少实现 feature-matching identity oracle

注意：
如果现有 trajectory models 不支持 feature 输入，不要强行改所有模型。
至少实现：
- PositionOnlyKNN
- FeatureAwareIdentityBaseline
- RandomIdentityBaseline

FeatureAwareIdentityBaseline：
- 根据可见 feature 直接匹配 object identity
- 在 feature-bearing setting 中应接近 1.0
- 在 featureless setting 中应接近 0.5

输出：
results/svt_v2_1_audit/identifiability_probe.csv

CSV 字段：
seed
setting
model
identity_accuracy
notes

解释规则：
- 如果 feature-bearing 大幅提升 identity，说明原 featureless 任务缺少可观测身份线索。
- 如果 feature-bearing 也不提升，说明 metric/pipeline 或模型输入处理存在问题。
- 如果 featureless 中所有非-oracle模型都接近 0.5，则不能简单说它们“缺少对象恒常性”，还要承认观测不可辨识。

========================
Audit Summary
========================

请生成：
results/svt_v2_1_audit/audit_summary.csv

每一行一个 audit：

字段：
audit_name
main_question
rawknn_identity_original
rawknn_identity_after_audit
identity_drop
diagnosis
severity

severity 可为：
- none
- mild
- serious
- critical

diagnosis 示例：
- object_order_leakage_likely
- absolute_position_shortcut_likely
- swap_template_shortcut_likely
- metric_bug_possible
- identity_task_not_identifiable
- rawknn_identity_remains_unexplained
- no_major_leakage_detected

========================
README.md 要求
========================

请生成：
results/svt_v2_1_audit/README.md

README 必须包含以下章节：

1. Purpose
   - 本审计解释 RawKNN v1 identity=0.830 的来源
   - 本审计不是 v3，不是新模型竞赛

2. Main Findings
   - 每个 audit 的一句话结论

3. RawKNN Identity Diagnosis
   - RawKNN 的高 identity 是否仍然成立？
   - 如果下降，最可能泄漏来源是什么？
   - 如果不下降，可能是什么真实线索？

4. Metric Sanity Check
   - label permutation 后 accuracy 是否接近随机？
   - 如果不是，必须标记为 critical

5. Identifiability
   - featureless task 是否可辨识？
   - feature-bearing objects 是否使 identity 可解？

6. Required Revision to SVT-v2 Report
   - 明确指出哪些原结论需要收紧
   - 禁止写 “all models fail above random identity”
   - 建议改为：
     “Most learned and delta-output models collapse toward random identity under identity-swap tests, while raw retrieval retains above-random identity under some settings, requiring leakage and identifiability analysis.”

7. Recommendation for SVT-v3
   - 是否可以进入 v3
   - v3 应优先修复什么
   - 是否需要 feature-bearing objects
   - 是否需要 disjoint train/test splits

========================
总脚本
========================

scripts/run_svt_v2_1_audit.py

运行后执行全部审计，并打印总览：

python scripts/run_svt_v2_1_audit.py

stdout 必须打印：

- RawKNN original identity
- RawKNN identity after object order randomization
- RawKNN identity after disjoint init split
- RawKNN identity after swap/occlusion randomization
- label permutation sanity result
- position ablation result
- identifiability result
- final recommendation:
  1. proceed_to_v3
  2. fix_metric_first
  3. fix_dataset_leakage_first
  4. redesign_identity_task_first

========================
严禁事项
========================

不要声称 SVT 通过。
不要声称所有模型 identity 都低于随机。
不要把 RawKNN v1 的高 identity 简单解释为结构。
不要把 RawKNN v1 的高 identity 简单解释为泄漏，除非审计结果支持。
不要进入 v3 非线性大实验。
不要新增复杂 neural models。
不要接入 LLM。

这是审计，不是胜利报告。

========================
完成标准
========================

运行：

python scripts/run_svt_v2_1_audit.py

必须生成：

results/svt_v2_1_audit/
  object_order_audit.csv
  disjoint_init_audit.csv
  swap_randomization_audit.csv
  label_permutation_audit.csv
  position_ablation_audit.csv
  nn_source_analysis.csv
  identifiability_probe.csv
  audit_summary.csv
  nn_distance_plot.png
  audit_fingerprint.png
  README.md

如果某些审计因为现有数据结构缺少字段无法完整实现：
- 不要静默跳过
- 在 stdout 和 README 中明确写明 skipped reason
- 仍然输出对应 CSV，字段填 NaN，并记录 reason